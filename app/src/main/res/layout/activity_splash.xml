<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="com.igordeoliveira.yolo.viewController.splash.SplashActivity">

    <ImageView
        android:layout_width="200dp"
        android:layout_height="300dp"
        android:id="@+id/imageView"
        android:layout_centerHorizontal="true"
        android:paddingTop="80dp"
        android:src="@drawable/logo"/>

    <TextView
        android:id="@+id/txtAppName"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="Your App Name"
        android:layout_below="@id/imageView"
        android:gravity="center"
        android:textSize="40sp"
        android:paddingTop="20dp"
        android:textColor="@color/Blue"
        />

    <TextView
        android:id="@+id/txtBeAwareTitle"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="Fique ligado!"
        android:layout_below="@id/txtAppName"
        android:gravity="center"
        android:textSize="15sp"
        android:textStyle="bold"
        android:paddingTop="20dp"
        android:textColor="@color/Gray"
        >
    </TextView>

    <TextView
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="Estamos trabalhando para melhorar o design! Será disponibilizado nas próximas versões!"
        android:layout_below="@id/txtBeAwareTitle"
        android:gravity="center"
        android:textSize="15sp"
        android:textStyle="italic"
        android:paddingTop="10dp"
        android:textColor="@color/Gray"
        >
    </TextView>

</RelativeLayout>
